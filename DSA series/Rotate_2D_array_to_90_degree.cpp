#include <bits/stdc++.h>
using namespace std;
/*vector<vector<int>> rotate_array(vector<vector<int>> v)
{
    vector<vector<int>> ans(3, vector<int>(3));
         int size=v.size();
    for (int index = 0; index <  v.size(); index++)
    {
        for (int j = 0; j < v[0].size(); j++)
        {

            ans[j][size-index-1] = v[index][j];
        }
    }
    return ans;
}*/
int main()
{
    vector<vector<int>> vect =
        {
            {1, 2, 3},
            {4, 5, 6},
            {7, 8, 9}};
         
   

    /*vector<vector<int>> result = rotate_array(vect);
    for (int i = 0; i < result.size(); i++)
    {
        for (int j = 0; j < result[0].size(); j++)
        {
            cout << result[i][j] << " ";
        }
        cout << endl;
    }*/
    
    return 0;
}